# Local scratch / throwaway work (not tracked)
scratch/

# Paper / documentation working files (not tracked)
docs/
!docs/
!docs/ui_input_design.md
!docs/starting_new_session_section.tex
!docs/check_step_section.tex
!docs/jax_step_section.tex
!docs/run_step_section.tex
!docs/diagnose_step_section.tex
lib/LLM/reference/study_input_example.md

# Python caches
__pycache__/
*.pyc

# personal git auth (never commit)
git-auth.config
*.credentials
.pfit-personal-pat
*personal-pat*

# generated tool logs
tools/*.log

# coverage artifacts
.coverage
htmlcov/

# root-level plot output from tools/plot_fits.py (regenerate with the tool).
# NOTE: anchored with a leading slash on purpose -- a bare "outputs/" would also
# match sessions/*/outputs and examples/*/outputs, which ARE tracked.
/outputs/

# blind re-run working notes (exercise record, not part of the workflow)
rerun_scorecard.md

# source papers pulled in while reconstructing models (large binaries)
*.pdf

# stray run logs
report.log

# Local Python environment
/venv/
