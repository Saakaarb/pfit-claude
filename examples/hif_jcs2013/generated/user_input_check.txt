Validation Report — hif_jcs2013
=====================================

Critical Errors
---------------
None detected.

Warnings
--------
1. [user_model.py / _compute_loss_problem] The total loss is loss_stab + loss_luc, where each
   component is individually normalized to [0, 1]. Their sum can reach ~2 for a poor fit
   if both observables were active simultaneously for the same experiment. In this session,
   each experiment uses mutually exclusive masks (mask_stab XOR mask_luc = 1), so the
   effective maximum per experiment is ~1 and strict normalization is maintained. No action
   required unless you add an experiment that measures both HIF stabilisation and luciferase
   simultaneously — in that case, divide the sum by 2.

Number of critical errors: 0
