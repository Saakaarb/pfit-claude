[pytest]
testpaths = tests
# Full PSO/NODE fits take tens of seconds each. They are the only way to cover
# the optimizer/solver configurations end to end, so they stay in the default
# run; deselect them with:  pytest -m "not slow"
markers =
    slow: runs a complete parameter fit (tens of seconds)
