tests:

- name: "run_driver_robertson"
  function: run_driver_wrapper
  input: ["tests/robertson_session"]
  expected:   # or leave blank if just checking for no error

- name: "run_driver_vanderpol"
  function: run_driver_wrapper
  input: ["tests/vanderpol_session"]
  expected:   # or leave blank if just checking for no error

